export type UserType = {
_id:string;
first_name:string;
last_name:string;
email:string;
gender:string;
}

export type VitaminType = {
    name: string,
        calories: number,
        fat: number,
        carbs: number,
        protein: number,
}